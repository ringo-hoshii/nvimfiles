# How to install
>[!NOTE]
>Make sure that the `nvim` folder does not exist. Otherwise Git might not let you clone

## Linux
```bash
git clone https://github.com/ringo-hoshii/nvimfiles ~/.config/nvim
```

## Windows
```cmd
git clone https://github.com/ringo-hoshii/nvimfiles %LocalAppData%\nvim
```

# Todo
- using the terminal with Vim motions (doesn't work on phone and won't on pc as well, i'm quite sure. but i need to find a way)
- vim-fugitive for git (gitgutter is just a gutter, not a complete git "interface" i think)
- vim-gitgutter plugin
